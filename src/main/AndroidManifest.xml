<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoGym"
        tools:targetApi="31">
        <activity

            android:name=".maincode.EkranStartowyActivity"
            android:theme="@style/AppTheme"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".maincode.HistoriaTreningowActivity"
            android:theme="@style/AppTheme"
            android:label="Statystyki" />
        <activity android:name=".maincode.KalkulatorBMRActivity"
            android:theme="@style/AppTheme"
            android:label="Kalkulator BMI" />
        <activity android:name=".maincode.LawkaPlaskaActivity"
            android:theme="@style/AppTheme"
            android:label="Trening" />
        <activity android:name=".maincode.MartwyCiagActivity"
            android:theme="@style/AppTheme"
            android:label="Trening" />
        <activity android:name=".maincode.PodciaganieActivity"
            android:theme="@style/AppTheme"
            android:label="Trening" />
        <activity android:name=".maincode.PodsumowanieActivity"
            android:theme="@style/AppTheme"
            android:label="Koniec Treningu" />
        <activity android:name=".maincode.RozpocznijTreningActivity"
            android:theme="@style/AppTheme"
            android:label="Rozpocznij Trening" />

    </application>

</manifest>